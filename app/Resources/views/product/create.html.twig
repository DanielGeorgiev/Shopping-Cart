{% extends 'base.html.twig' %}

{% block title%}Add Product{% endblock %}

{% block main %}
    <div class="container body-content span=8 offset=2">
        <div class="well">
            <form class="form-horizontal" action="{{ path('product_add') }}" method="POST">
                <fieldset>
                    <legend>New Product</legend>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="product_name">Name *</label>
                        <div class="col-sm-4 ">
                            <input type="text" class="form-control" minlength="3" maxlength="255" required id="product_name" placeholder="Product Name"
                                   name="product[name]">
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="product_desc">Description</label>
                        <div class="col-sm-6">
                            <input type="text" class="form-control" minlength="10" maxlength="255" id="product_desc" placeholder="Product Description"
                                   name="product[description]">
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="product_price">Price *</label>
                        <div class="col-sm-4 ">
                            <input type="number" step="0.01" min="0.5" max="10000" required class="form-control" id="product_price" placeholder="Product Price"
                                   name="product[price]">
                        </div>
                    </div>


                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="product_category">Category *</label>
                        <div class="col-sm-4">
                            {{ form_widget(form.category) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="product_quantity">Quantity *</label>
                        <div class="col-sm-4">
                            <input type="number" min="1" max="200" required class="form-control" id="product_quantity" placeholder="Product Quantity"
                                   name="product[quantity]">
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-4">
                            <a class="btn btn-default" href="{{ path('home_page') }}">Cancel</a>
                            <button type="submit" class="btn btn-primary">Add new product</button>
                        </div>
                    </div>

                    {{ form_row(form._token) }}
                </fieldset>
            </form>
        </div>
    </div>
{% endblock main %}